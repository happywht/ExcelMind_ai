# 🚀 快速启动指南

## 前置条件

确保已安装 Node.js (推荐 v18+) 和 npm。

## 5 分钟快速启动

### 步骤 1: 安装依赖

```bash
npm install
```

### 步骤 2: 启动 API 服务器

```bash
npm run dev:api
```

你将看到如下输出：

```
============================================================
ExcelMind AI - Phase 2 API 服务器
============================================================
环境: development
启动中...

✓ 服务器已启动

  服务地址:
    HTTP:  http://localhost:3001
    WS:    ws://localhost:3001/api/v2/stream

  健康检查:
    GET    http://localhost:3001/health

  API 端点:
    数据质量: http://localhost:3001/api/v2/data-quality
    模板管理: http://localhost:3001/api/v2/templates
    批量生成: http://localhost:3001/api/v2/generation
    审计规则: http://localhost:3001/api/v2/audit

  WebSocket 频道:
    - task_progress
    - generation_status
    - audit_alerts
    - performance_alerts

按 Ctrl+C 停止服务器
============================================================
```

### 步骤 3: 测试 API

打开新的终端窗口，运行快速测试：

```bash
npx tsx scripts/quick-api-test.ts
```

你将看到测试结果：

```
============================================================
🚀 ExcelMind AI - API 快速测试
============================================================

🔍 测试健康检查端点...
✅ 健康检查通过
   版本: 2.0.0
   运行时间: 123秒

🔍 测试数据质量分析...
✅ 数据质量分析通过
   分析ID: qa_1737792000000_abc123
   质量分数: 85
   问题数量: 2

🔍 测试模板列表...
✅ 模板列表获取成功
   模板数量: 2
   第一个模板: 销售合同模板

🔍 测试批量任务创建...
✅ 批量任务创建成功
   任务ID: task_1737792000000_def456
   状态: pending
   预计文档数: 100

🔍 测试 WebSocket 连接...
✅ WebSocket 连接成功
✅ WebSocket 订阅成功
   订阅频道: task_progress

============================================================
📊 测试结果汇总
============================================================
health                ✅ 通过
dataQuality           ✅ 通过
templates             ✅ 通过
batchGeneration       ✅ 通过
websocket             ✅ 通过

============================================================

🎉 所有测试通过! (5/5)
```

## 🎯 恭喜！

如果看到所有测试都通过，说明 API 服务器已成功运行！

## 下一步

### 选项 1: 启动前端

在另一个终端窗口：

```bash
npm run dev
```

然后访问 http://localhost:3000

### 选项 2: 同时启动前端和 API

```bash
npm run dev:full
```

### 选项 3: 使用 Mock 服务器（推荐用于前端开发）

```bash
npm run dev:mock
```

## 📚 更多信息

- [API 集成指南](server/API_INTEGRATION_GUIDE.md) - 详细的 API 使用文档
- [Day 2 完成报告](DAY2_API_INTEGRATION_README.md) - 完整的任务总结
- [集成完成总结](PHASE2_INTEGRATION_SUMMARY.md) - 技术实现细节

## 🔧 常用命令

```bash
# 服务器
npm run dev:api              # 启动 API 服务器
npm run dev:full             # 同时启动前端和 API
npm run dev:mock             # 启动 Mock 服务器
npm run server:start         # 启动生产服务器

# 测试
npm run test:integration:api # 运行集成测试
npm run test:phase2:ui       # 使用 UI 运行测试
npm run test:phase2:watch    # 监视模式运行测试

# 快速测试
npx tsx scripts/quick-api-test.ts
```

## 💡 提示

1. **端口冲突**: 如果 3001 端口被占用，可以设置环境变量：
   ```bash
   API_PORT=3002 npm run dev:api
   ```

2. **查看日志**: 服务器会输出详细的请求日志，方便调试。

3. **热重载**: 使用 `tsx watch` 启动的服务器支持代码修改后自动重启。

4. **Mock 数据**: 前端开发时可以使用 Mock 服务器，不需要后端服务。

## 🆘 遇到问题？

### 问题 1: 端口被占用
```bash
# Windows (PowerShell)
netstat -ano | findstr :3001
# 然后使用 taskkill 结束进程

# Linux/Mac
lsof -ti:3001 | xargs kill -9
```

### 问题 2: 依赖安装失败
```bash
# 清除缓存重新安装
npm cache clean --force
rm -rf node_modules package-lock.json
npm install
```

### 问题 3: TypeScript 错误
```bash
# 确保 TypeScript 版本正确
npm install typescript@~5.8.2
```

---

**开始开发吧！** 🎉
