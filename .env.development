# 开发环境变量配置
# 此文件用于开发环境,不会被提交到版本控制

# ==========================================
# API 服务配置
# ==========================================

# API服务地址 (开发环境)
# 修复：移除 /api 后缀，避免路径重复
# Vite代理会自动将 /api 请求转发到后端
# 如果使用完整URL，应该是后端的根地址
VITE_API_BASE_URL=http://localhost:3001/api/v2

# ==========================================
# CORS 配置
# ==========================================

# 允许的跨域请求来源（多个用逗号分隔）
# 开发环境默认允许本地前端
CORS_ORIGIN=http://localhost:3000,http://127.0.0.1:3000

# ==========================================
# 认证配置
# ==========================================

# 开发环境禁用认证 (仅用于测试)
AUTH_ENABLED=false

# 开发环境API密钥 (如果启用认证)
# API_KEYS=dev-test-key-12345,another-test-key-67890

# ==========================================
# WebSocket 配置
# ==========================================

# WebSocket 服务地址（相对路径）
VITE_WS_BASE_URL=ws://localhost:3001/api/v2/stream
